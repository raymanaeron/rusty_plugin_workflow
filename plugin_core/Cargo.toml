[package]
name = "plugin_core"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = { version = "0.7", features = ["macros", "http1", "tokio"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1"
libc = "0.2"
